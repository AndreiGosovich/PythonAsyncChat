# Каждое из слов «class», «function», «method» записать в байтовом типе без преобразования в последовательность кодов
# (не используя методы encode и decode) и определить тип, содержимое и длину соответствующих переменных.

print('Задание 2:')

var_1 = b'class'
var_2 = b'function'
var_3 = b'method'

print(var_1, type(var_1), 'len:', len(var_1))
print(var_2, type(var_2), 'len:', len(var_2))
print(var_3, type(var_3), 'len:', len(var_3))

print('=================================================')
